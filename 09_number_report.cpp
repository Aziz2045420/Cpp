#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

/*
Inspired by: sum.cpp, gcd.cpp, vectors.cpp

Task:
1. Implement gcd(int a, int b) as in the professor's example.
2. Implement sum_vector(const vector<int>& values).
3. Read an integer n.
4. Read n integers into a vector.
5. Print:
   sum=<sum>
6. If n > 0, also print:
   gcd_first_last=<gcd of first and last element>
7. Sort the vector and print all values on one line with the label sorted:
*/

int gcd(int a, int b)
{
    // TODO: Same idea as in gcd.cpp.
    return 0;
}

int sum_vector(const vector<int>& values)
{
    // TODO: Sum all vector elements and return the result.
    return 0;
}

int main()
{
    int n = 0;

    cout << "n: ";
    // TODO: Read n.

    vector<int> values(n);

    // TODO: Read n integers into the vector.

    // TODO: Print the sum.

    // TODO: If n > 0, print the gcd of the first and last element.

    // TODO: Sort the vector.

    // TODO: Print the sorted vector with label sorted: .

    return 0;
}
